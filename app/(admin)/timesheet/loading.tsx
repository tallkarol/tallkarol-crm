import { BrandLoader } from "@/components/BrandLoader"

/** See app/(admin)/loading.tsx — this layout's children need their own boundary. */
export default function Loading() {
  return <BrandLoader />
}
