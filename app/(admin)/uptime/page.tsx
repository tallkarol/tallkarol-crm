import { ComingSoon } from "@/components/ComingSoon"

export const metadata = { title: "Uptime" }

export default function UptimePage() {
  return (
    <ComingSoon
      title="Uptime"
      description="Health checks for every site and app in the portfolio — status, response time, and incident history per client. The uptime audits you already bill for, running continuously."
    />
  )
}
