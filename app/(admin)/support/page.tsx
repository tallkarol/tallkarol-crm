import { ComingSoon } from "@/components/ComingSoon"

export const metadata = { title: "Support Tickets" }

export default function SupportPage() {
  return (
    <ComingSoon
      title="Support Tickets"
      description="Centralized inbox for support requests from every app you've built — form fills, emails, and in-app reports funnel here, linked to the client and billable against their retainer."
    />
  )
}
