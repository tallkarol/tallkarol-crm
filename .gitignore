# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local
# `.env*.local` requires the name to END there, so a .env.local.bak — the kind
# of thing an editor or a rotation script leaves behind — was committable.
.env*.local.*

# vercel / railway
.vercel
.railway

# typescript
*.tsbuildinfo
next-env.d.ts

# service account keys — never commit
*-key.json
service-account*.json
gcp-*.json
/.next-build/
/.next-merge/
/.next-smoke/
/.next-review/
/.next-wait/
